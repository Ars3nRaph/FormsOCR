version: "3.9"
services:
  ocrstudio:
    build: .
    ports: ["8000:8000"]
    environment:
      - JWT_SECRET=${JWT_SECRET:-change-me}
      - DEFAULT_PLAN=${DEFAULT_PLAN:-free}
    volumes:
      - ./app/data:/app/app/data
